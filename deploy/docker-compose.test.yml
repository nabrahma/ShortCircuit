# Containerised test run. Requires NO credentials.
#   docker compose -f deploy/docker-compose.test.yml up --exit-code-from tests
name: shortcircuit-test

services:
  postgres:
    image: postgres:16
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
      POSTGRES_DB: shortcircuit_test
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U postgres"]
      interval: 5s
      timeout: 5s
      retries: 10
    tmpfs:
      - /var/lib/postgresql/data     # ephemeral; nothing persists between runs

  tests:
    build:
      context: ..
      dockerfile: deploy/Dockerfile
    depends_on:
      postgres: { condition: service_healthy }
    environment:
      DB_HOST: postgres
      DB_USER: postgres
      DB_PASS: postgres
      DB_NAME: shortcircuit_test
      DB_PORT: "5432"
      PYTHONPATH: /app/src
    entrypoint: ["/bin/sh", "-c"]
    command:
      - |
        pip install --no-cache-dir -q pytest pytest-asyncio pytest-cov hypothesis freezegun &&
        pytest tests/unit tests/property -v \
          --cov=shortcircuit --cov-report=term
