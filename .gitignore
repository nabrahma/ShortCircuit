# ============================================================
# ShortCircuit .gitignore
# RULE: If it contains credentials or is machine-specific → ignore
# ============================================================

# ── Credentials & Tokens (NEVER commit) ──────────────────────
.env
access_token.txt
data/access_token.txt
data/*.txt
*.token
*.key

# ── Python Cache ──────────────────────────────────────────────
__pycache__/
*.py[cod]
*$py.class
*.pyc
*.pyo
.pytest_cache/
.venv/
.agents/
.swarm/

# ── Logs (machine-specific, not code) ─────────────────────────
logs/
*.log
*.log.*

# ── Database Files (local only) ───────────────────────────────
*.db
*.sqlite
*.sqlite3

# ── IDE & OS Noise ────────────────────────────────────────────
.vscode/settings.json
.idea/
*.DS_Store
Thumbs.db
desktop.ini

# ── Debug/Verify Scripts (not production code) ────────────────
verification/
# Also ignore the generated report if generic
terminal_log.md
md/*
!md/PHASE44_5_OPEN_ITEMS.md

# ── Runtime Files ─────────────────────────────────────────────
shortcircuit.lock

# ── Data Directory (runtime data, not code) ───────────────────
data/

# ── Build Artifacts ───────────────────────────────────────────
dist/
build/
*.egg-info/

# ── Legacy / Unused Code (moved from root) ───────────────────
legacy/

